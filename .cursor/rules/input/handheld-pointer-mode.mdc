---
description: "掌机摇杆逻辑指针模式：最终行为、关键状态与源码入口"
alwaysApply: false
globs:
  - "src/onsyuri/ONScripter_event.cpp"
  - "src/onsyuri/ONScripter.cpp"
  - "src/onsyuri/ONScripter.h"
  - "src/onsyuri/ONScripter_image.cpp"
---

# 摇杆光标指针模式（最终实现）

面向 Linux 掌机（常无物理鼠标、Wayland）：左摇杆驱动引擎内逻辑坐标 `pointer_cursor_x/y`，自绘光标贴图；与传统手柄方向键/键盘路径并存。

## 状态与默认值

- `input_mode`：`INPUT_MODE_TRADITIONAL` | `INPUT_MODE_POINTER`
- `pointer_cursor_x/y`：脚本分辨率下的逻辑指针，移动时钳制在 `[0, screen_width-1]` × `[0, screen_height-1]`
- `pointer_axis_deadzone`：轴死区约 20%（32767 基准）
- `pointer_axis_max_step`：`ONScripter` 构造函数里设为 **15**（摇杆推满时每帧最大像素步进上限）
- `pointer_idle_timeout_ms`：5000；`last_left_stick_active_ms` 用于空闲退出指针模式

`resetSub()` 会把 `pointer_cursor_*` 与 `current_button_state.x/y` 一并设为 **屏幕中心**，避免首次进入指针模式时被旧的 `current_button_state` 拉偏。

## 生命周期（均在 `runEventLoop()` 内 lambda）

1. **进入指针模式**：左摇杆超出死区 → `input_mode = INPUT_MODE_POINTER`；若刚从传统模式切入，先 `syncPointerFromCurrentState()`（从当前 `current_button_state` 对齐指针），并刷新空闲计时。
2. **移动**：`drivePointerByCurrentStick()` 每轮 `SDL_JoystickUpdate()` 后读 **实时轴**（优先 `SDL_GameControllerGetAxis`，否则 `SDL_JoystickGetAxis` 轴 0/1），经死区、`axisToStep`、可选矢量尾部 snap 后更新 `pointer_cursor_*`，再 `mouseMoveEvent` 合成坐标（`WAIT_BUTTON_MODE` 下仍会走原有悬停逻辑）。
3. **空闲退出**：`updatePointerIdleMode()` 超时则回到传统模式并刷新光标区域。
4. **实体十字键退出**：`SDL_JOYHATMOTION` 与 `SDL_CONTROLLER_BUTTON_DPAD_*` 触发 `exitPointerModeForPhysicalDpad()`。

摇杆有位移或指针模式下有交互时通过 `markPointerInteraction()` 刷新 `last_left_stick_active_ms`（含合成点击、手柄按键轮询等）。

## 合成左键与悬停

`pointerLeftClick()`：

1. `markPointerInteraction`
2. `syncPointerMouseOver()`：把 `current_button_state` 对齐到指针坐标，并 **无条件** `mouseOverCheck(pointer_cursor_x, pointer_cursor_y)`，避免剧情/UI 小按钮场景下 `mousePressEvent` 仍用陈旧 `current_over_button` 退化成空白左键
3. 构造 `SDL_MouseButtonEvent` 调用 `mousePressEvent`

## GameController 与 Raw Joystick（物理 A = 确认/左键）

在 Nintendo 类物理布局上，SDL 抽象键位与壳子标注常见错位：**合成左键绑定在 `SDL_CONTROLLER_BUTTON_B`**（对应多数设备上的物理 A）。其余键仍经 `transControllerButton()` 走键盘路径。

Raw Joystick（Loong 表注释 `0=B 1=A …`）：指针模式下 **固定用按钮索引 `1`** 触发 `pointerLeftClick`（事件分支与 `pollJoystickButtons` 轮询一致）。

`transControllerButton()` 仍将 **GUIDE** 映射为 `SDLK_F10`；**已不再**在 `keyPressEvent()` 的 `KEYUP` 分支用 F10 切换全屏（避免 Menu/GUIDE 误触）。桌面仍可用 **Alt+Return** 切换全屏。

## 绘制与刷新

- `input_mode == INPUT_MODE_POINTER` 时，`REFRESH_CURSOR_MODE` 始终绘制 `cursor_info[0]`（见 `ONScripter_image.cpp`）
- 指针移动时用光标 **旧矩形与新矩形并集** 作为 dirty rect 调用 `flush`，避免每次整屏刷新

## 修改时注意

- 改确认键或映射时同时核对 GameController 与 Raw Joystick 两条路径，以及 `pollJoystickButtons` 特判。
- `refreshMouseOverButton()` 仍用系统鼠标坐标；逻辑指针场景依赖移动/点击路径里的 `mouseOverCheck`，不要轻易假定 OS 指针与 `pointer_cursor_*` 一致。
