---
description: "Linux 掌机摇杆驱动游戏内逻辑指针的现状、已知问题与修改注意点"
alwaysApply: false
globs:
  - "src/onsyuri/ONScripter_event.cpp"
  - "src/onsyuri/ONScripter.cpp"
  - "src/onsyuri/ONScripter.h"
  - "src/onsyuri/ONScripter_image.cpp"
  - "摇杆与指针模式问题总结.md"
---

# 掌机摇杆与指针模式

背景：目标平台是 Linux ARM 掌机（Wayland、无物理鼠标）。左摇杆应驱动引擎内逻辑指针，指针模式下按确认键应等效左键；摇杆空闲一段时间后回到传统模式；实体 D-pad 应退出指针模式并隐藏光标。

## 当前实现

- 主逻辑在 `ONScripter::runEventLoop()` 内部 lambda：`updatePointerByStick`、`drivePointerByCurrentStick`、`pointerLeftClick`、`updatePointerIdleMode`、`exitPointerModeForPhysicalDpad`。
- `drivePointerByCurrentStick()` 每轮读取实时轴值，不依赖单个 `SDL_CONTROLLERAXISMOTION` / `SDL_JOYAXISMOTION` 事件位移；轴事件分支主要缓存状态或保留传统方向键行为。
- 指针坐标是 `pointer_cursor_x/y`，移动后更新 `cursor_info[0].pos` 并 `flush(refreshMode() | REFRESH_CURSOR_MODE, &screen_rect)`；绘制侧只有 `input_mode == INPUT_MODE_POINTER` 时强制绘制 `cursor_info[0]`。
- 空闲退出只看 `last_left_stick_active_ms` 与 `pointer_idle_timeout_ms`。当前只有进入指针模式或实际产生 `dx/dy` 位移时刷新计时；摇杆仍出死区但步进为 0 的帧不会续时。
- `mouseMoveEvent()` 在 `WAIT_BUTTON_MODE` 下调用 `mouseOverCheck()` 更新 `current_over_button`；`mousePressEvent()` 左键分支直接使用已有 `current_over_button`，不会自行按点击坐标重算悬停。

## A/B 与输入路径

- GameController 路径：`transControllerButton()` 采用 Nintendo 式对调，`SDL_CONTROLLER_BUTTON_A -> SDLK_ESCAPE`，`SDL_CONTROLLER_BUTTON_B -> SDLK_RETURN`。但在 `INPUT_MODE_POINTER` 下，`SDL_CONTROLLER_BUTTON_A` 会被提前拦截为 `pointerLeftClick()`，不会走键盘路径。
- Raw Joystick 路径：`transJoystickButton()` 中 Loong 布局为 `button 0(B) -> SDLK_ESCAPE`、`button 1(A) -> SDLK_RETURN`。指针模式下拦截条件是“映射为 `SDLK_ESCAPE` 的按钮”，因此 Raw Joystick 下实际触发合成左键的是 ESC 映射按钮。
- 按键命中 UI 按钮依赖 `current_over_button` 是否与 `pointer_cursor_x/y` 同步。若此前没有 `mouseMoveEvent()` 更新悬停，或 `refreshMouseOverButton()` 用系统鼠标位置重置了悬停，合成左键可能退化为 `LCLICK` / 推进文本。

## 已知风险

- `waitEventSub()` 只调用一次 `runEventLoop()`；而 `runEventLoop()` 在无事件分支和循环末尾会因为 `drivePointerByCurrentStick()` 返回 true 直接 `return`。持续摇杆移动可能打断等待定时动画的节拍。
- 每次指针步进都整屏刷新 cursor mode，可能与 `timerEvent()` / `proceedAnimation()` 在同一线程竞争，掌机上容易表现为 UI 滚动或过场动画卡顿。
- D-pad 退出路径已存在：`SDL_JOYHATMOTION` 与 `SDL_CONTROLLER_BUTTON_DPAD_*` 会调用 `exitPointerModeForPhysicalDpad()`。若实机异常，先区分当前走 GameController 还是 Raw Joystick。

## 修改原则

- 修复摇杆问题时先对照 `摇杆与指针模式问题总结.md`，不要把已撤销的实验性补丁当作现状。
- 优先小步改动并在实机验证：动画等待是否被摇杆提前返回打断、A/B 是否按目标产品语义命中按钮、光标空闲隐藏是否符合 UI 场景预期。
- 若改点击命中，重点考虑在合成左键前同步 `pointer_cursor_x/y` 与 `mouseOverCheck()`；若改动画卡顿，重点评估 `drivePointerByCurrentStick()` 的返回语义和指针刷新频率。
